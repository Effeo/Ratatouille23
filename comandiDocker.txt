#avviare il conteiner del db nella rete
docker run --name mysqldb --network springboot-mysql-net -e MYSQL_ROOT_PASSWORD=1234 -e MYSQL_DATABASE=ratatouille23DB -e 
MYSQL_USER=effeo -e MYSQL_PASSWORD=1234 -d mysql:5.7

#comunicare col db
docker exec -it "id container" bash

#crea immagine springboot
docker build -t springbootmysql .

#eseguire il conteiner di springboot nella stessa rete del db
docker run --network springboot-mysql-net --name springboot-container -p 8080:8080 -d springbootmysql